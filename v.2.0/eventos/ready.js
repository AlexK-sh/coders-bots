const Discord = require('discord.js');

module.exports = {
	name: 'ready',//nombre del evento
async execute(message, client) { 

    console.log("El bot se ha encendido, mnesaje enviado desde el event handler")

}
}
